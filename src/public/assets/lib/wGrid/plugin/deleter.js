import { util } from './util.js';
import { constant } from "./constant.js";
import { reposit } from "./reposit.js";
import { status } from "./status.js";
import { creator } from "./creator.js";

/**
 * 그리드 삭제 관련 객체
 */
export const deleter = {

    /**
     * 그리드 삭제 관련 객체 초기설정
     * @param {*} self
     * @returns 
     */
    init: (self) => init(self),

    removeRowElement: (self, rowIdx, rowSeq) => {}
}

const init = (self) => {


}